import {Characters, Pagination} from "../components";

const CharactersPage = () => {
    return (
        <div>
            <Characters/>
            <Pagination/>
        </div>
    );
};

export {CharactersPage};